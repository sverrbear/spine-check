# subs/

One JSON file per person, exactly as the app downloads it
(`spine-03-bjorn.json`). Drop them in here, commit, and the Results
tab picks them up on its own — no pasting.

Committing over an existing file, or adding a second file for the same
spine number, replaces that person's earlier ratings.
